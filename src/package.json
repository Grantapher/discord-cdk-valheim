{
  "name": "src",
  "version": "1.0.0",
  "description": "lambda functions",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "postbuild": "cp package*.json dist && cd dist && npm install --only=production"
  },
  "author": "grantapher",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.9.0",
    "@aws-sdk/client-ec2": "^3.9.0",
    "@aws-sdk/client-ecs": "^3.9.0",
    "discord-interactions": "^2.0.2",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.72",
    "@types/node": "^14.14.35",
    "@types/node-fetch": "^2.5.8",
    "aws-sdk": "^2.868.0",
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  }
}
